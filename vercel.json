{
  "version": 2,
  "devCommand": "uvicorn api.index:app --reload --host 0.0.0.0 --port 8000",
  "builds": [
    {
      "src": "api/*.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/index.py"
    }
  ]
}
